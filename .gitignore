node_modules/

# The composer output src/cards/<card>/<card>.html IS committed (deterministic).
# The rendered PDFs are a regenerable build output — not committed, because
# WeasyPrint randomises font-subset tags so they aren't byte-stable.
output/*.pdf

# Cached bracket images (regenerated on demand by renderBracket)
assets/bracket-*.b64

*.log
.DS_Store
